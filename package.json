{
    "name": "chrome-lag-test",
    "version": "0.0.1",
    "description": "",
    "scripts": {
        "dev": "esbuild src/main.js --outfile=public/bundle.js --target=es6 --servedir=public --bundle --define:DEBUG=true",
        "prod": "esbuild src/main.js --outfile=public/bundle.js --minify --target=es6 --bundle --define:DEBUG=false",
        "prod-dev": "npm run prod && netlify dev"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/aforren1/esbuild-test.git"
    },
    "bugs": {
        "url": "https://github.com/aforren1/esbuild-test/issues"
    },
    "license": "MIT",
    "homepage": "https://github.com/aforren1/esbuild-test#readme",
    "devDependencies": {
        "esbuild": "^0.14.43"
    }
}
